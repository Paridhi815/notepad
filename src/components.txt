->container
    ->header
    ->board
        ->title
            ->title note
            ->text title 
        ->en button
        ->note
            ->note note
            ->note text
        ->save
    ->footer